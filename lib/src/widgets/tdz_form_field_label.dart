import 'package:flutter/material.dart';

class TDZFormFieldLabel extends StatelessWidget {
  const TDZFormFieldLabel({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}